# Environment

Approved by the user's P2 execution request on 2026-08-10.

- Frozen inputs are generated by `evals/chat_agent_p2/dataset.py`.
- OpenRouter is live because tool choice must be measured on the production model.
- Required credential: `OPENROUTER_API_KEY` through project Settings.
- Tools are schemas only; all backing services and write effects are absent.
